<project name="user-database" default="compile">

  <property name="pack.name" value="user-database" />
  <property name="pack.version" value="1-0-0" />
  <property name="pack.dir" value="/vobs/eniq/design/plat/admin_ui/dev/adminui_userdb" />

  <!-- LIBRARY PATHS -->
  <property name="3pp.dir" value="/vobs/eniq/3pp" />

  <!-- TOMCAT LIBRARIES ARE USED FOR BUILDING ONLY! -->
  <property name="servlet-api.dir" value="${3pp.dir}/apache/tomcat/v7_0_14/prod" />

  <target name="precompile" />

  <target name="compile">
    <mkdir dir="${pack.dir}/classes" />
    <javac srcdir="${pack.dir}/src" destdir="${pack.dir}/classes" failonerror="true" debug="on" optimize="off" deprecation="off">
      <classpath>
        <pathelement path="${classpath}" />
        <pathelement path="${servlet-api.dir}/servlet-api.jar" />
        <pathelement path="${servlet-api.dir}/catalina.jar" />
        <pathelement path="${servlet-api.dir}/tomcat-coyote.jar" />
        <pathelement path="${servlet-api.dir}/tomcat-juli.jar" />
        <pathelement path="${servlet-api.dir}/tomcat-util.jar" />
      </classpath>
    </javac>
  </target>

  <target name="postcompile">
    <copy todir="${pack.dir}/classes">
      <fileset dir="${pack.dir}/src">
        <include name="**/*.xml" />
      </fileset>
    </copy>
  </target>

</project>